{
  "name": "basketball-project",
  "private": true,
  "workspaces": [
    "combined-app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace combined-app",
    "build": "npm run build --workspace combined-app",
    "preview": "npm run preview --workspace combined-app"
  }
}
